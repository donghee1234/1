GCD는 두 수 a와 b에 대하여 a 연산을 반복해서 나머지가 0이 될 때 b값을 반환하는 최대공약수 산출 방식입니다.

한 번의 연산을 수행할 때마다 새로운 숫자는 원래 숫자의 절반이하로 줄어드는 특성을 가짐.
숫자가 기하급수적으로 감소하기 때문에 연산 횟수는 숫자의 자릿수에 비례함.

Big-O 계산 
O(log(min(a.b)))


공간 복잡도 
공간 복잡도는 O(1) < 상수시간

GCD 알고리즘은 $O(\log N)$ 의 시간 복잡도를 가짐.

///////////////////////////////////////////

피보나치 알고리즘 복잡도

시간 복잡도 분석
 
피보나치: 한 번의 함수 호출이 두 번의 재귀 호출로 이어짐 데이터가 1증가할 때마다 연산량이 2배씩 늘어남

GCD 알고리즘: 나머지 연산을 통해 크기가 빠르게 줄어든다. 연산 횟수가 가장 많지만 로그 시간 내에 계산이 완료된다


<img width="815" height="628" alt="image" src="https://github.com/user-attachments/assets/fca22f23-7ff0-481c-bc5b-72fdca4f7231" />

프로파일링 분석

n이 30을 넘어서면서 CPU 점유율이 크게 상승했습니다.
그리고 스택 메모리 사용량이 n에 비례하여 증가하는 거 처럼 보였습니다.

재귀 피보나치는 입력값이 커질수록 성능이 저하되지만 복잡도를 가진 GCD 알고리즘은 효율적으로 작동하는걸 보았습니다.
